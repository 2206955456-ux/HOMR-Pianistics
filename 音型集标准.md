# 音型（音群）集标准

> 本文档是 scoreflow 分析框架的**音群特征判定标准**（教学口径），是代码实现与人工标注的共同依据。
> 版本 v1.1，对应代码版本 v1.4.0。

## 1. 概述

《音型（音群）集》定义了一组可在乐谱符号层（MusicXML / music21）直接观测的**钢琴演奏技术音群特征**，
用于把曲目内容量化为可比的难度指标。每个特征由「窗口大小 + 命中判据 + 难度加权」三要素构成，
三要素可全部通过 `config.yaml` 配置，不改引擎代码即可启用、调参或扩展。

**统计口径**：以**小节**为单位，对单声部音符序列做滑动窗口匹配；小节内任一窗口命中即该小节命中；
多声部任一声部命中即算。**双手（两声部）在同一小节同时出现某技术时，该技术难度加倍**（见第 3 节）。

## 2. 六类音群特征

| 特征（代码名） | 窗口 | 命中判据 | 难度加权 |
|---|---|---|---|
| 伸张把位（`stretched_grip`） | 3 音 | 相邻 3 音音高走向相同（单调递增或递减），且前后两段音程之和 > 12 半音（八度） | 2 |
| 收缩把位（`contracted_grip`） | 5 音 | 相邻 4 个音程之和 < 6 半音（减五度） | 1 |
| 连续十六分音符跑动（`consecutive_sixteenths`） | 5 音 | 单声部出现连续 5 个十六分音符 | 2 |
| 连续八分音符跑动（`consecutive_eighths`） | 3 音 | 单声部出现连续 3 个八分音符 | 1 |
| 连续和弦支撑（`consecutive_chords`） | 3 和弦 | 单声部出现连续 3 个和弦（≥ 3 音叠置） | 2 |
| 连续音程支撑（`consecutive_intervals`） | 3 音程 | 单声部出现连续 3 个双音叠置（音程） | 1 |

## 3. 双手拆分诊断

钢琴跨谱表琶音（如车尔尼 Op.740 No.21 第 1 小节）中，快速分解和弦实际由左右手交替演奏。
OMR 输出可能把双手音符归入同一声部/谱表，形成单声部内跨 10 度以上（≥16 半音）的音程。

**诊断开关**：`analysis.hand_split_threshold`（默认 16 半音 = 十度）。当某小节合并所有
谱表后的音符跨度 ≥ 阈值时，触发双手拆分：按每个 onset 的最大音程间隙把音符切分为
右手（高音组）与左手（低音组），重建为两个独立声部后再做特征检测。

该诊断是声部正确性的预处理步骤，不影响六类特征的判定口径本身。

## 4. 难度加权模型

难度总分：

```
D = Σ_f  w_f · ( m_f^1 + 2 · m_f^2 )
```

- `w_f`：特征 `f` 的难度加权（上表末列）；
- `m_f^1`：特征 `f` 仅单手出现的命中小节数；
- `m_f^2`：特征 `f` 双手（两声部）同节出现的命中小节数。

规则含义：双手在同一小节内同时出现某技术，演奏难度叠加，故该小节该技术按 `2 · w_f` 计分。
这一加权模型把乐谱统计直接映射为**可比的量化难度分**，为分级选曲提供排序依据。

## 5. 参数对照（config.yaml）

| 代码名 | 关键参数 | 默认值 | 说明 |
|---|---|---|---|
| `stretched_grip` | `threshold` / `monotonic` / `arpeggio_aware` | 12 / true / false | 半音阈值；单调方向条件；琶音感知豁免开关（消融用，默认关） |
| `contracted_grip` | `threshold` | 6 | 4 个音程之和的上界（半音） |
| `consecutive_sixteenths` | `window_size` | 5 | 连续音符个数 |
| `consecutive_eighths` | `window_size` | 3 | 连续音符个数 |
| `consecutive_chords` | `window_size` / `min_notes` | 3 / 3 | 连续和弦个数；和弦 = ≥ 3 音叠置 |
| `consecutive_intervals` | `window_size` / `exact` | 3 / true | 连续音程个数；严格要求 2 音叠置 |

> 另有 `big_single_leap`（单个大跳）作为预留特征，默认关闭，不在本标准六类之内。

## 6. 语料与人工标注

完整语料为中国音乐学院社会艺术水平考级教程 **1–10 级练习曲 10 首**（共 400 小节），
人工标注以整曲计数粒度逐曲统计六类特征的命中小节数，作为金标准。

| 编号 | 考级等级 | 小节数 |
|---|---|---|
| etude01 | 1 级 | 10 |
| etude02 | 2 级 | 24 |
| etude03 | 3 级 | 24 |
| etude04 | 4 级 | 58 |
| etude05 | 5 级 | 36 |
| etude06 | 6 级 | 48 |
| etude07 | 7 级 | 32 |
| etude08 | 8 级 | 39 |
| etude09 | 9 级 | 45 |
| etude10 | 10 级 | 79 |

完整标注明细见仓库内 `标注_10首.xlsx`（六类特征 × 10 首，含逐特征命中小节数）。
系统检测与人工标注的一致率（60 单元）约 **93.1%**，其中连续十六分跑动特征达 **98.6%**。

## 7. 扩展方式

新增音群特征仅需在 `scoreflow/analysis/features/` 下复制 `_template.py`，实现 `match()`，
声明 `name` / `group_size` / `weight`，并在 `config.yaml` 的 `analysis.features` 段启用即可，
无需改动引擎代码。
