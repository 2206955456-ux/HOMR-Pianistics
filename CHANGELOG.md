# Changelog

本仓库的版本更新记录。语义化版本：`主.次.补丁`。

## v1.2.0 (2026-08-31)

### 问题
- **琶音织体上的宽跳系统性误报**：`wide_leap_sum` 的复合音程判据（相邻 3 音两段
  音程之和 > 八度）会把分解和弦跑动误判为旋律宽跳。人工核查证实 etude7 几乎不存在
  真实旋律宽跳（通篇为琶音跑动 + 左手和弦），但基线命中占比高达 96.9%（31/32 小节）；
  全语料 806 条命中音组中 702 条（87.1%）为琶音音型。

### 新增功能
- **琶音感知开关**（`analysis.features.wide_leap_sum.params.arpeggio_aware`，
  默认关闭，向后兼容）
  - 判据为**和弦归属**（chord membership）而非音程方向：音群 3 音的音级集合若可
    整体嵌入某个三和弦或常用七和弦（任意转位/八度/次序，允许重复音），判为分解
    和弦音型并豁免；
  - 选择和弦归属而非单调方向过滤的原因：误报中大量是折返型琶音
    （如 `A-2 -> D-2 -> A-2`，7+7=14 半音），方向过滤无法覆盖
- **论文级对比报告**（`arpeggio_compare.py`）
  - 输出「基线 vs 开启开关」对比 Excel（5 张表：对比总览 / 命中分解 / 逐小节明细 /
    其他特征校验 / 全语料汇总）与 Markdown 版，两版引擎均启用交叉手重分配，
    单变量对比，可直接放入论文
  - 内置自洽校验：开关后命中音组 == 基线命中中的非琶音音组（逐页 Counter 比对）

### 数据结果（4 首中国音乐学院考级 7–10 级练习曲，195 小节）

| 乐谱 | 基线占比 | 开关后 | Δ | 基线命中中琶音占比 |
|:---|:---:|:---:|:---:|:---:|
| etude7 | 96.9% | 25.0% | -71.9pp | 94.5% |
| etude8 | 71.1% | 2.6% | -68.4pp | 98.0% |
| etude9 | 75.0% | 38.6% | -36.4pp | 80.7% |
| etude10 | 77.8% | 44.4% | -33.3pp | 80.3% |
| 全语料 | 79.0% | 31.8% | -47.2pp | 87.1% |

- 其余特征（consecutive_sixteenths / consecutive_chords）逐页 Δ=0，开关无副作用
- 详见 `output/reports/琶音感知对比_论文版.xlsx`

### 已知限制（本版不彻底解决）
- 音群混入非和弦音（经过音/倚音）时判定失效，仍会误报（etude7 残留 14 条，如
  `E2 -> B2 -> A3`，A 为 E 大三和弦外音）；
- 声部交错音群可能不满足和弦归属，仍会误报；
- 真旋律宽跳若恰为同一和弦和弦音也会被豁免（和弦归属为必要不充分条件）。
- 完整解决方向：调性和声分析（和弦还原）+ 声部分离

### 测试
- 新增 `is_arpeggio` 判定、开关行为、引擎集成共 4 组单元测试（11/11 通过）

## v1.1.0 (2026-08-31)

### 新增功能
- **交叉手启发式声部重分配**（`--cross-hand-heuristic` / `config.yaml: analysis.cross_hand_heuristic`）
  - 新模块 `scoreflow/analysis/cross_hand.py`
  - 规则：低音谱表(staff2)上 > C5(midi 72) 的音符 → 重标为右手（voice 1 / staff 1）；
    高音谱表(staff1)上 < C3(midi 48) 的音符 → 重标为左手（voice 5 / staff 2）
  - 用于缓解 HOMR 未保留 `<stem>` 标签导致的交叉手段落声部错配问题
- **论文级对比报告**（`cross_hand_compare.py`）
  - 输出「基线 vs 重分配」对比 Excel（5 张表：对比总览 / 按特征汇总 / 逐页命中 /
    重分配音符明细 / 声部分布）与 Markdown 版，可直接放入论文
- **人工标注一致性评估**（`agreement_eval.py`）
  - 生成标注模板（`标注模板_4首.xlsx`，40 小节）+ 计算 P / R / F1 / Cohen's Kappa

### Bug 修复
- **声部重复计数**：`score.parts` 返回惰性迭代器（StreamIterator），被 `len()` 消费后
  迭代状态错乱，导致同一高音声部产出两次（Piano#1 = Piano#2）、命中记录虚高一倍；
  修复为先物化 `list(score.parts)` 再迭代
- **总览合计行公式**：单曲时引用错行；多曲时占比不可直接相加，改为不输出合计行避免误导

### 数据结果（4 首中国音乐学院考级 7–10 级练习曲，195 小节）
- 全语料重分配 118 个音符（LH→RH 105、RH→LH 13）
- etude7 的 wide_leap_sum 命中率 90.6% → 96.9%（+6.2pp）
- etude8 的 consecutive_sixteenths 命中率 86.8% → 89.5%（+2.6pp）
- 详见 `output/reports/交叉手对比_论文版.xlsx`

### 文档
- README 新增「交叉手问题与启发式重分配」一节与「已知限制」

## v1.0.0 (2026-08-30)

- 初始版本：PDF → HOMR 识别 → music21 特征分析 一站式工作流
- 内置 4 个可插拔音群特征（wide_leap_sum / big_single_leap / consecutive_sixteenths / consecutive_chords）
- 报告输出：report.md / report.json / report.xlsx（总览 + 每特征明细）
